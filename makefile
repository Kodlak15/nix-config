everest:
	sudo ./scripts/rebuild.sh everest --nix

denali:
	sudo ./scripts/rebuild.sh denali --nix

rainier:
	sudo ./scripts/rebuild.sh rainier --nix

everest-home:
	./scripts/rebuild.sh everest --home

denali-home:
	./scripts/rebuild.sh denali --home

rainier-home:
	./scripts/rebuild.sh rainier --home
