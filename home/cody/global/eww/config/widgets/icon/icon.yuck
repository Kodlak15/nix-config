(defwidget icon [action image ?class]
  (button
    :class "${class}"
    :onclick "${action}"
    :style "background: url('${image}'); background-repeat: no-repeat;"
    :halign "center"
    :valign "center"
  )
)
