(defwidget power []
  (eventbox
    :onclick "$(which eww) open dashboard --config $HOME/.config/eww/bars/default/"
    :class "power-event"
    :timeout "10s"
    (box
      :class "power"
      (label :text "⏻")
    )
  )
)
