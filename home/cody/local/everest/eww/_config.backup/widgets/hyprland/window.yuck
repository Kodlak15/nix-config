(deflisten window 
  :initial "..." 
  "sh $HOME/.config/eww/scripts/hyprland.sh -t"
)

(defwidget window_w []
  (eventbox
    :class "window_w-event"
    (box
      :class "window_w"
      (label 
        :text "${window}"
        :limit-width 60
      )
    )
  )
)
